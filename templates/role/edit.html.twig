{% extends 'base.html.twig' %}

{% block title %}Modifier le rôle{% endblock %}

{% block body %}
    <div class="containerFormEdit">
        <h1 class="newFormEditTitle">Modifier le rôle</h1>

        {{ include('role/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="containerFormNewElementButtons">
            <div class="itemFormNewSubmitButton itemFormDelete">{{ include('role/_delete_form.html.twig') }}</div>
        </div>
        <div class="containerFormNewButtons">
            <a class="formNewBackButton" href="{{path('home_index')}}">Retour</a>
        </div>
    </div>
{% endblock %}
