{{ form_start(form) }}
{#     {{ form_widget(form) }} #}
    <div class="containerFormNewElement">
        <div class="itemFormNewTextArea">{{ form_row(form.comment) }}</div>
    </div>
    <div class="containerFormNewElement">
        <div class="itemFormNewSelect">{{ form_row(form.rate) }}</div>
    </div>
    <div class="containerFormNewElementButtons">
        <button class="itemFormNewSubmitButton">{{ button_label|default('Ajouter') }}</button>
    </div>
{{ form_end(form) }}
