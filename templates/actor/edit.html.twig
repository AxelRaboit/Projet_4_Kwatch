{% extends 'base.html.twig' %}

{% block title %}Modifier l'acteur{% endblock %}

{% block body %}
    <div class="containerFormEdit">
        <h1 class="newFormEditTitle">Modification de l'acteur</h1>
        {{ include('actor/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        <div class="containerFormNewElementButtons">
            <div class="itemFormDelete">{{ include('actor/_delete_form.html.twig') }}</div>
        </div>
        <div class="containerFormNewButtons">
            <a class="formNewBackButton" href="{{path('home_index')}}">Retour</a>
        </div>
    </div>
{% endblock %}
