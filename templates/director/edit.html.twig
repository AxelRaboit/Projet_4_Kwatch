{% extends 'base.html.twig' %}

{% block title %}Modifier le réalisateur{% endblock %}

{% block body %}
    <div class="containerFormEdit">
        <h1 class="newFormEditTitle">Modifier le réalisateur</h1>

        {{ include('director/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="containerFormNewElementButtons">
            <div class="itemFormNewSubmitButton itemFormDelete">{{ include('director/_delete_form.html.twig') }}</div>
        </div>
    </div>
{% endblock %}
